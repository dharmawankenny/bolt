import palette from './PrayaPalette';
import spacing from './PrayaSpacing';
import border from './PrayaBorder';
import typography from './PrayaTypography';

const Praya: Theme = {
  palette,
  spacing,
  border,
  typography,
  fontFace: 'font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Oxygen", "Ubuntu", "Helvetica Neue", Arial, sans-serif',
};

export default Praya;
